version: "3"
services:
  server:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - "/usr/src/app/node_modules"
      - ".:/usr/src/app"
    restart: always